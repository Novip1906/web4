services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: web4-backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend/src/data:/app/src/data
    restart: unless-stopped
